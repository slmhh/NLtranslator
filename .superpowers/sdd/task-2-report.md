### Task 2 Report: Config module

**Status:** Complete

**Commits:**
- `757f9b6` — feat: add config module and .env.example

**Files created:**
- `.env.example` — environment variable template with NapCat URLs, bot QQ, and reply messages
- `bot/config.ts` — `Config` interface, `ReplyMessages` interface, `loadConfig()` function, and `parseEnvFile()` helper

**Concerns:** None — both files written exactly per the brief and verified.
